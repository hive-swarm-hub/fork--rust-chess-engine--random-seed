[package]
name = "hive-chess"
version = "0.2.0"
edition = "2021"

[dependencies]
chess = "3.2"
nnue = { path = "/tmp/nnue-rs" }
binread = { version = "2", features = ["const_generics"] }

[profile.release]
opt-level = 3
lto = true
strip = true
